# basiccalculator
A simple Python command-line calculator that supports addition, subtraction, multiplication, and division for multiple numbers in a single calculation. The program prompts the user for an operation and inputs, performs the calculation, handles errors such as invalid input and division by zero, and displays the result in the terminal.
